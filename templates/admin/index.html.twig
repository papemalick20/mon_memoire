{% extends 'base.html.twig' %}

{% block title %}'Gerer les biens!'{% endblock %}

{% block body %}

 <div class="container mt-4">
 <h1> gerer les biens</h1>
 {% for message in app.flashes('success') %}
 <div class="alert alert-success">
      {{ message }}
      </div>
      {%  endfor %}
 <table class ="table table-striped">
 <thead>
  <tr>
   <th>Titre</th>
   <th>Action</th>
  </tr>
 </thead>
 <tbody>
 {% for property in properties %}
  <tr>
   <td>{{property.title}}</td>
   
     <td>
     <a href ="{{path('admin.edit',{id:property.id}) }}" class="btn btn-secondary">Editer</a>
     <form method ="post" action="{{path('admin.delete',{id : property.id})}}"
     style="display:inline-block" onsubmit="return confirm('Etes vous sur de supprimer')">
     <input type="hidden" name="_method" value="DELETE">
     <button class="btn btn-danger">Suprimer</button>
     </form>
     </td>
     </tr>
 {% endfor %}
 </tbody>
 </table>
  <div class="text-right">
   <a href = "{{path('admin.new')}}" class="btn btn-primary">Créer un nouveau bien</a>
 </div> 
</div> 
{% endblock %}
